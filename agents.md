# Agent Guidelines

- Document each change clearly in the final response so humans understand the impact.
- After making any code edits, run `.\gradlew.bat build` (or `./gradlew build` on Unix) to ensure the project still compiles before handing the work back.
